import { RestictnameDirective } from './restictname.directive';

describe('RestictnameDirective', () => {
  it('should create an instance', () => {
    const directive = new RestictnameDirective();
    expect(directive).toBeTruthy();
  });
});
