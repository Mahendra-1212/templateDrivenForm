import { AsynDirective } from './asyn.directive';

describe('AsynDirective', () => {
  it('should create an instance', () => {
    const directive = new AsynDirective();
    expect(directive).toBeTruthy();
  });
});
