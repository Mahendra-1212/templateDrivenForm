import { CvalidationDirective } from './cvalidation.directive';

describe('CvalidationDirective', () => {
  it('should create an instance', () => {
    const directive = new CvalidationDirective();
    expect(directive).toBeTruthy();
  });
});
