import { StructDirTestDirective } from './struct-dir-test.directive';

describe('StructDirTestDirective', () => {
  it('should create an instance', () => {
    const directive = new StructDirTestDirective();
    expect(directive).toBeTruthy();
  });
});
